/target
# mdbook render output (book/ is the source; book/book/ is generated)
/book/book
# Assembled Pages site (mdbook guide + rustdoc), produced only in CI
/site
# Bytecode the interpreter caches beside the consistency gates' scanners
/ci/__pycache__
