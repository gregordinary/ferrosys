# API reference

The complete API reference is generated by rustdoc and published alongside this
guide:

<a href="api/ferrosys/index.html">Open the ferrosys API reference &rarr;</a>

To build it locally from the workspace root:

```sh
cargo doc --no-deps --open
```
